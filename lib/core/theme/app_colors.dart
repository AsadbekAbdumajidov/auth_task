import 'package:flutter/material.dart';

// ---- base of application colors ----

class AppColors {
  AppColors._();
  static const backgroundColor = Color.fromRGBO(243, 243, 246, 10);
  static const black = Color(0xFF000000);
  static const primaryColor = Color(0xFF4631D2);
  static const white = Color(0xFFFFFFFF);
  static const white2 = Color(0xFFF8F8F9);
  static const secondaryDark = Color.fromRGBO(235, 235, 245, 0.6);
  static const transparent = Colors.transparent;
  static const greyColor = Color(0xFF929292);
  static const red = Colors.red;
}
